spring:
  datasource:
    url: jdbc:mysql://localhost:3300/IMS_Dev?createDatabaseIfNotExist=true
    username: root
    password: poop
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
